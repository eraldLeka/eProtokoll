@using eProtokoll.Models
@{
    ViewData["Title"] = "Dashboard";
}

<div class="container-fluid">
    <!-- Welcome Header -->
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="mb-1">
                <i class="bi bi-house-door me-2 text-primary"></i>
                Mirë se vini, @User.Identity?.Name
            </h2>
            <p class="text-muted">Përmbledhja e aktivitetit - @DateTime.Now.ToString("dd MMMM yyyy")</p>
        </div>
    </div>

    <!-- Dokumentet e Fundit & Veprime të Shpejta -->
    <div class="row mb-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="bi bi-clock-history me-2 text-primary"></i>
                        Dokumentet e Fundit
                    </h5>
                    <a asp-area="Manager" asp-controller="ProtocolBook" asp-action="Index" class="btn btn-sm btn-outline-primary">
                        Libër Protokolli <i class="bi bi-arrow-right"></i>
                    </a>
                </div>
                <div class="card-body p-0">
                    @if (ViewBag.RecentDocuments != null && ((IEnumerable<Document>)ViewBag.RecentDocuments).Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th>Protokolli</th>
                                        <th>Subjekti</th>
                                        <th>Lloji</th>
                                        <th>Data</th>
                                        <th class="text-end">Veprime</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var doc in (IEnumerable<Document>)ViewBag.RecentDocuments)
                                    {
                                        <tr>
                                            <td><strong>@doc.ProtocolNumber</strong></td>
                                            <td>
                                                <div class="text-truncate" style="max-width:350px" title="@doc.Subject">@doc.Subject</div>
                                            </td>
                                            <td>
                                                @switch (doc.DocumentType)
                                                {
                                                    case DocumentType.Incoming:
                                                        <span class="badge bg-info"><i class="bi bi-arrow-down-circle"></i> Hyrës</span>
                                                        break;
                                                    case DocumentType.Outgoing:
                                                        <span class="badge bg-success"><i class="bi bi-arrow-up-circle"></i> Dalës</span>
                                                        break;
                                                    case DocumentType.Internal:
                                                        <span class="badge bg-warning text-dark"><i class="bi bi-arrow-left-right"></i> Brendshëm</span>
                                                        break;
                                                }
                                            </td>
                                            <td><small>@doc.CreatedDate.ToString("dd/MM/yyyy")</small></td>
                                            <td class="text-end">
                                                <a asp-area="Manager" asp-controller="ProtocolBook" asp-action="Details"
                                                   asp-route-id="@doc.DocumentId" asp-route-type="@doc.DocumentType"
                                                   class="btn btn-sm btn-outline-primary">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="bi bi-inbox text-muted" style="font-size:3rem"></i>
                            <p class="text-muted mt-3">Nuk ka dokumente</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Veprime të Shpejta -->
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-lightning-charge me-2"></i>Veprime të Shpejta</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-area="Manager" asp-controller="IncomingDocument" asp-action="Create" class="btn btn-info">
                            <i class="bi bi-arrow-down-circle me-2"></i>Dokument Hyrës
                        </a>
                        <a asp-area="Manager" asp-controller="OutgoingDocument" asp-action="Create" class="btn btn-success">
                            <i class="bi bi-arrow-up-circle me-2"></i>Dokument Dalës
                        </a>
                        <a asp-area="Manager" asp-controller="InternalDocument" asp-action="Create" class="btn btn-warning">
                            <i class="bi bi-arrow-left-right me-2"></i>Dokument Brendshëm
                        </a>
                        <hr />
                        <a asp-area="Manager" asp-controller="Tracking" asp-action="Create" class="btn btn-outline-purple">
                            <i class="bi bi-geo-alt me-2"></i>Cakto Dokument
                        </a>
                        <a asp-area="Manager" asp-controller="ProtocolBook" asp-action="Index" class="btn btn-outline-primary">
                            <i class="bi bi-book me-2"></i>Libër Protokolli
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Aktiviteti i 7 Ditëve -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white">
                    <h5 class="mb-0">
                        <i class="bi bi-bar-chart-line me-2 text-primary"></i>
                        Aktiviteti i 7 Ditëve të Fundit
                    </h5>
                </div>
                <div class="card-body">
                    @{
                        var dailyStats = ViewBag.DailyStats as IEnumerable<dynamic>;
                        int maxVal = dailyStats != null && dailyStats.Any()
                        ? (int)dailyStats.Max(d => (int)d.Count)
                        : 1;
                        if (maxVal == 0) maxVal = 1;
                    }
                    @if (dailyStats != null && dailyStats.Any())
                    {
                        <div style="display:flex; align-items:flex-end; gap:8px; height:160px; padding:0 8px;">
                            @foreach (var day in dailyStats)
                            {
                                int heightPct = (int)Math.Round((double)(int)day.Count / maxVal * 100);
                                bool isToday = day.Date == DateTime.Now.ToString("dd/MM");
                                <div style="flex:1; display:flex; flex-direction:column; align-items:center; gap:3px; height:100%; justify-content:flex-end;">
                                    <span style="font-size:11px; color:#6c757d;">@(((int)day.Count) > 0 ? day.Count.ToString() : "")</span>
                                    <div style="width:60%; height:@(heightPct == 0 ? 2 : heightPct)%; min-height:2px; background-color:@(isToday ? "#0d6efd" : "#9ec5fe"); border-radius:4px 4px 0 0;"></div>
                                    <span style="font-size:10px; color:#6c757d; white-space:nowrap;">@day.Date</span>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-center text-muted">Nuk ka të dhëna.</p>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .card {
            border-radius: 12px
        }

        .card-header {
            border-radius: 12px 12px 0 0 !important
        }

        .text-purple {
            color: #6f42c1 !important
        }

        .btn-outline-purple {
            border-color: #6f42c1;
            color: #6f42c1
        }

            .btn-outline-purple:hover {
                background-color: #6f42c1;
                color: #fff
            }
    </style>
}
